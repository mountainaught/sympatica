# Sympatica: API and Frontend for the Empatetica E4 Wristband
Work in progress, written in Vue and Django.

Since the original application and cloud solution for this product was sunset in early 2025, I figured I'd port it to a locally run application. The wristwatch itself only collects sensor data and returns them as responses to BLE packets. 
